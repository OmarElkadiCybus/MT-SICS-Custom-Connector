{
  "$id": "MtsicsEndpoint",
  "definitions": {
    "address": {
      "title": "MT-SICS command configuration",
      "description": "Defines the command verb and optional arguments sent to the scale.",
      "oneOf": [
        {
          "type": "string",
          "enum": [
            "S",
            "SI",
            "TA",
            "PCS",
            "PW",
            "REF",
            "@",
            "TAC",
            "T",
            "Z"
          ]
        },
        {
          "type": "object",
          "additionalProperties": false,
          "required": ["command"],
          "properties": {
            "command": {
              "type": "string",
              "enum": [
                "S",
                "SI",
                "TA",
                "PCS",
                "PW",
                "REF",
                "@",
                "TAC",
                "T",
                "Z"
              ]
            },
            "args": {
              "type": "array",
              "items": {
                "type": "string"
              },
              "description": "Optional command arguments appended after the verb."
            }
          }
        }
      ]
    }
  }
}
