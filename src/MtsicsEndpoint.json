{
  "$id": "MtsicsEndpoint",
  "definitions": {
    "command": {
      "type": "object",
      "properties": {
        "command": {
          "$id": "/properties/command",
          "title": "command",
          "type": "string",
          "description": "The command to be sent to the scale",
          "enum": [
            "S",
            "SI",
            "TA",
            "PCS",
            "PW",
            "REF",
            "@",
            "TAC",
            "T",
            "Z"
          ]
        }
      }
    }
  },
  "type": "object",
  "properties": {
    "subscribe": { "$ref": "#/definitions/command" },
    "write": { "$ref": "#/definitions/command" },
    "read": { "$ref": "#/definitions/command" }
  }
}
